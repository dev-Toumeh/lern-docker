run those command in the root directory of the Project (the same directory where the doccker-compose.yaml is placed)
### Start docker compose
```sh
docker compose up -d
```

###  stop docker compose
```sh
docker compose stop
```
